# Net Conductor 测试记录

首次实机测试：2026-09-09。环境：Ubuntu 公网服务器 + Windows 11 台式机，接入既有 WireGuard 私网。

## 通过

- `go test ./...`、`go vet ./...`，Windows/Linux 构建，JavaScript 语法检查。
- 管理登录、跨站请求拒绝、设备令牌隐藏、本机 API 鉴权、空集合响应。
- 两台设备状态展示；真实心跳、WireGuard 握手及流量统计。
- Windows 应用 → 本地 HTTP 代理 → 服务器 → 台式机共享 SOCKS5 → 外部 HTTPS：204。
- 故意配置不可用的首选出口，后备台式机出口成功；测试后还原原策略。
- TCP/HTTP/UDP 映射通过服务器私网入口转发到台式机。
- **公网 HTTPS 管理接口、公网 TCP、HTTP、UDP 映射**。UDP 安全组放行后，公网回显验证成功。
- 浏览器交互式 SSH 登录台式机及公网服务器，执行 `whoami` 核对真实身份。
- Windows 服务安装、升级、自启动；独立桌面程序及托盘。
- 系统代理启用与恢复；核对原注册表代理设置已恢复。
- 一键修复重启真实 WireGuard 隧道后，代理请求恢复成功。
- 管理网页在 1440×900、1024×1366、768×1024、390×844 检查；桌面端实机截图；无 JavaScript 页面异常。

## 2026-09-10 服务器本机代理迁移

- 新增仅回环监听的 HTTP/SOCKS5 TCP 混合代理，共用公网服务器出口策略。
- 自动测试覆盖 HTTP 请求、HTTP CONNECT、SOCKS5 CONNECT、出口拒绝时禁止直连、非回环绑定拒绝和监听退出；Go 测试及 vet 通过。
- 临时端口实测 HTTP/SOCKS5 HTTPS 请求均返回 204；首选笔记本出口模拟故障后切换至台式机，恢复后重新使用笔记本。
- 正式入口复用原本机 17890；旧 Mihomo 服务、程序和配置删除，台式机旧 portproxy 清理。
- 使用服务器 Codex 进程的代理环境访问 OpenAI HTTPS 接口收到 401（未携带账户认证），证明网络连接，不等于完整模型任务验收。

## v0.1.1 登录自启与恢复

- Windows 安装器为目标用户配置 Startup 快捷方式，带 `--startup` 参数，登录后只进入托盘。已验证安装后的快捷方式目标和参数。
- Electron 实测 `--startup` 模式不创建窗口；后续打开正常加载连接概览。
- 自动测试覆盖停止隧道的启动、运行/启动中/停止中服务不被干预、查询失败不启动、失败退避上限及恢复重置、共享监听退出后清理并重建。
- 本机更新后后台在线、WireGuard 隧道就绪。未执行整机重启、断网或停止真实隧道测试。

## v0.2.0 网页远程功能（2026-09-11）

- Windows OpenSSH 配置脚本在已安装 OpenSSH 的实机重复执行成功；原授权密钥保留，本机密钥去重追加，服务端公钥通过带设备认证的 HTTPS 同步。
- Go 测试覆盖 WebRTC 双端直连 RPC、切到相同会话的 WebSocket 中转、未授权信令/公钥请求拒绝、Linux 桌面拒绝、授权文件去重与保留。
- Edge 浏览器实际连接 Windows SSH 并执行 whoami；直连与强制中转分别验证成功。
- 两种通道分别完成 SFTP 上传、下载内容核验、重命名、新建目录、文件删除和空目录删除。
- 两种通道分别接收 Windows 主屏幕画面；键盘输入在独立测试窗口验证，避免操作用户其他应用。Linux SSH 与 SFTP 列表验证通过。
- 修复原 UDP 转发测试使用 TCP 端口抽样的问题，避免 Windows 协议端口保留差异导致误报；Go 测试及 vet 通过。
- 未做整机重启或断网测试；强制中转通过测试浏览器禁用直连候选实现，没有断开设备网络。未在缺少 OpenSSH 的全新 Windows 机器验收 Windows 功能下载安装路径。

## 其他待验证范围

- 无既有 WireGuard 的全新机器完整安装入网、多台额外设备的长期运行。
- HTTPS 域名自动签发、跨平台客户端和多用户权限不在当前实现范围内。

临时测试映射会在脚本结束时删除。测试用防火墙规则在验证结束后清理。原有 WireGuard、Mihomo、Caddy 配置已备份，原网络继续运行。
